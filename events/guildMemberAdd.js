module.exports  = async (member, guild) => {
    // const WelcomeChanel = guild.channels.cache.find(channel => channel.name === "welcome")
    // WelcomeChanel.send(`Welcome ${member} to ${guild.name}`) 
}